# `Hold`

`Hold` is a git repos status displayer that recursively inspects status of repos under current path, and outputs extensive details and git statuses of all children git repos under current directory.
